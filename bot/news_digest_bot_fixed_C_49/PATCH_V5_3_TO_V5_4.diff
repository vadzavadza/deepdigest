--- v53base/newsbot_v5_3_single_run_orchestrator/app/search_v2/strategy.py	2026-04-26 09:32:36.000000000 +0000
+++ newsbot_v5_4_retrieval_reliability/app/search_v2/strategy.py	2026-04-26 10:19:39.583545123 +0000
@@ -45,7 +45,8 @@
 
 _KNOWN_BRANDS: Final = {
     "apple", "tesla", "microsoft", "google", "alphabet", "amazon", "meta", "netflix", "nvidia",
-    "bmw", "mercedes", "volkswagen", "toyota", "samsung", "sony", "disney",
+    "bmw", "mercedes", "volkswagen", "toyota", "samsung", "sony", "disney", "dyson",
+    "openai", "oracle", "intel", "amd", "ibm", "salesforce", "adobe", "spotify", "uber", "airbnb",
 }
 _KNOWN_CRYPTO: Final = {"bitcoin", "ethereum", "solana", "xrp", "cardano", "dogecoin", "binance", "toncoin", "polkadot"}
 
@@ -62,6 +63,15 @@
     "kharkiv": {"kharkiv", "kharkov", "харків", "харьков"},
     "odesa": {"odesa", "odessa", "одеса", "одесса"},
     "dnipro": {"dnipro", "dnieper", "дніпро", "днепр"},
+    # Global city aliases are generic normalization, not per-test behavior.
+    "paris": {"paris", "париж", "paris france", "парис"},
+    "rome": {"rome", "roma", "рим", "rome italy"},
+    "london": {"london", "лондон", "london uk"},
+    "berlin": {"berlin", "берлин", "берлін"},
+    "madrid": {"madrid", "мадрид"},
+    "barcelona": {"barcelona", "барселона"},
+    "tbilisi": {"tbilisi", "tiflis", "тбилиси", "თბილისი"},
+    "new york": {"new york", "nyc", "нью-йорк", "нью йорк"},
 }
 
 _CITY_CONTEXTS: Final[dict[str, dict[str, set[str]]]] = {
@@ -70,6 +80,14 @@
     "kharkiv": {"positive": {"ukraine", "ukrainian", "kharkiv oblast"}, "negative": set()},
     "odesa": {"positive": {"ukraine", "ukrainian", "black sea", "odesa oblast"}, "negative": {"texas", "florida", "missouri", "delaware", "permian", "high school", "weather spark"}},
     "dnipro": {"positive": {"ukraine", "ukrainian", "dnipropetrovsk"}, "negative": set()},
+    "paris": {"positive": {"france", "french", "ile-de-france", "île-de-france", "mayor", "city hall"}, "negative": {"paris hilton", "paris texas", "texas"}},
+    "rome": {"positive": {"italy", "italian", "lazio", "mayor", "city council"}, "negative": {"ancient rome", "roman empire"}},
+    "london": {"positive": {"uk", "united kingdom", "england", "mayor", "city hall"}, "negative": {"london ontario", "ontario"}},
+    "berlin": {"positive": {"germany", "german", "senate", "brandenburg"}, "negative": set()},
+    "madrid": {"positive": {"spain", "spanish", "city council"}, "negative": set()},
+    "barcelona": {"positive": {"spain", "catalonia", "catalan", "city council"}, "negative": set()},
+    "tbilisi": {"positive": {"georgia", "georgian", "caucasus"}, "negative": set()},
+    "new york": {"positive": {"nyc", "new york city", "mayor", "manhattan", "brooklyn"}, "negative": {"new york state"}},
 }
 
 _AMBIGUOUS_ENTITY_HINTS: Final[dict[str, dict[str, set[str]]]] = {
@@ -88,10 +106,53 @@
     },
 }
 
+_ENTITY_ALIASES: Final[dict[str, str]] = {
+    # Common public-name shorthand and multilingual spellings. This is normalization only;
+    # ranking still requires direct topical evidence from each article.
+    "trump": "donald trump",
+    "donald trump": "donald trump",
+    "дональд трамп": "donald trump",
+    "трамп": "donald trump",
+    "pavel durov": "pavel durov",
+    "durov": "pavel durov",
+    "павел дуров": "pavel durov",
+    "павло дуров": "pavel durov",
+    "дуров": "pavel durov",
+}
+
+_BRAND_ALIASES: Final[dict[str, str]] = {
+    "dyson": "dyson",
+    "дайсон": "dyson",
+    "дайсан": "dyson",
+    "microsoft": "microsoft",
+    "майкрософт": "microsoft",
+    "volkswagen": "volkswagen",
+    "vw": "volkswagen",
+    "фольксваген": "volkswagen",
+    "tesla": "tesla",
+    "тесла": "tesla",
+    "openai": "openai",
+    "опенай": "openai",
+    "apple": "apple",
+    "эппл": "apple",
+}
+
+# Simple deterministic transliteration helps the search layer try both scripts. It is not
+# used as proof of relevance; it only generates more reliable query variants.
+_CYRILLIC_TRANSLIT: Final[dict[str, str]] = {
+    "а": "a", "б": "b", "в": "v", "г": "g", "д": "d", "е": "e", "ё": "yo", "є": "ie", "ж": "zh",
+    "з": "z", "и": "i", "і": "i", "ї": "yi", "й": "y", "к": "k", "л": "l", "м": "m", "н": "n",
+    "о": "o", "п": "p", "р": "r", "с": "s", "т": "t", "у": "u", "ф": "f", "х": "kh", "ц": "ts",
+    "ч": "ch", "ш": "sh", "щ": "shch", "ъ": "", "ы": "y", "ь": "", "э": "e", "ю": "yu", "я": "ya",
+    "ґ": "g",
+}
+
 _TRANSLIT_ALIASES: Final[dict[str, str]] = {}
 for canonical, aliases in _CITY_ALIASES.items():
     for alias in aliases:
         _TRANSLIT_ALIASES[alias] = canonical
+_TRANSLIT_ALIASES.update(_ENTITY_ALIASES)
+_TRANSLIT_ALIASES.update(_BRAND_ALIASES)
 
 
 def _normalize(text: str) -> str:
@@ -99,9 +160,42 @@
     return text
 
 
+def transliterate_cyrillic(text: str) -> str:
+    chars: list[str] = []
+    for ch in text.lower():
+        chars.append(_CYRILLIC_TRANSLIT.get(ch, ch))
+    return re.sub(r"\s+", " ", "".join(chars)).strip()
+
+
 def canonicalize_topic(query: str) -> str:
     q = _normalize(query)
-    return _TRANSLIT_ALIASES.get(q, q)
+    if q in _TRANSLIT_ALIASES:
+        return _TRANSLIT_ALIASES[q]
+    translit = transliterate_cyrillic(q)
+    if translit and translit != q and translit in _TRANSLIT_ALIASES:
+        return _TRANSLIT_ALIASES[translit]
+    return translit if translit and translit != q else q
+
+
+def _query_bases(raw_query: str, canonical: str, kind: str) -> list[str]:
+    raw_norm = _normalize(raw_query)
+    translit = transliterate_cyrillic(raw_norm)
+    bases: list[str] = []
+    for candidate in (canonical, raw_norm, translit):
+        candidate = _normalize(candidate)
+        if candidate and candidate not in bases:
+            bases.append(candidate)
+
+    if kind == "city":
+        context = _CITY_CONTEXTS.get(canonical, {}).get("positive", set())
+        # Add one disambiguated city form early, e.g. "paris france" or "odesa ukraine".
+        for term in sorted(context):
+            if len(term.split()) <= 2 and term not in {"mayor", "capital", "city hall", "city council"}:
+                disambiguated = f"{canonical} {term}"
+                if disambiguated not in bases:
+                    bases.insert(1, disambiguated)
+                break
+    return bases[:4]
 
 
 def _tokens(query: str) -> set[str]:
@@ -149,6 +243,8 @@
         return "game_updates"
     if q in _KNOWN_CRYPTO or any(h in q for h in _CRYPTO_HINTS):
         return "crypto"
+    if q in set(_ENTITY_ALIASES.values()):
+        return "person"
     if _looks_like_software_tech(q):
         return "technology"
     if _looks_like_esports_query(query, q):
@@ -184,16 +280,12 @@
 
 
 def _freshness_hint(from_dt: datetime, to_dt: datetime, first_run: bool) -> str:
+    # Hard provider-side date constraints often make OpenRouter return zero links.
+    # Retrieve a slightly wider candidate pool, then enforce real freshness locally.
     hours = max(int((to_dt - from_dt).total_seconds() // 3600), 1)
-    since = from_dt.strftime("%Y-%m-%d")
-    if hours <= 30:
-        return f"past 24 hours published after {since}"
-    if hours <= 54:
-        return f"past 48 hours published after {since}"
-    if hours <= 96:
-        return f"past 72 hours published after {since}"
-    # First runs can use a wider window, but never a vague "recent" query.
-    return f"past {max(1, hours // 24)} days published after {since}"
+    requested_days = max(1, (hours + 23) // 24)
+    retrieval_days = min(30, max(7, requested_days * 2 if first_run else requested_days + 3))
+    return f"recent news from the last {retrieval_days} days"
 
 
 def _semantic_hints_for_query(q: str, kind: str) -> set[str]:
@@ -232,14 +324,20 @@
 
 
 def _alternate_terms(q: str) -> set[str]:
+    terms = {q}
     if q in _CITY_ALIASES:
-        return set(_CITY_ALIASES[q])
+        terms |= set(_CITY_ALIASES[q])
+    for alias, canonical in _ENTITY_ALIASES.items():
+        if canonical == q:
+            terms.add(alias)
+    for alias, canonical in _BRAND_ALIASES.items():
+        if canonical == q:
+            terms.add(alias)
     if q in _TECH_SOFTWARE_HINTS:
-        terms = {q, f"{q} programming language", f"{q} software"}
+        terms |= {q, f"{q} programming language", f"{q} software"}
         if q == "python":
             terms |= {"python programming", "python software", "python release", "pypi"}
-        return terms
-    return {q}
+    return {t for t in terms if t}
 
 
 def _context_terms(q: str, kind: str) -> tuple[set[str], set[str]]:
@@ -254,6 +352,8 @@
         positive |= {"software", "programming", "developer", "release", "security", "open source", "github", "package"}
         if q == "python":
             negative |= {"snake", "zoo", "reptile", "burmese python", "reticulated python"}
+    if kind == "business":
+        positive |= {"company", "product", "launch", "earnings", "ceo", "shares", "market", "regulation", "deal"}
     ambiguous = _AMBIGUOUS_ENTITY_HINTS.get(q)
     if ambiguous:
         for hints in ambiguous.values():
@@ -281,12 +381,20 @@
     exact_phrase = q if len(q) >= 3 else None
     strict_phrase = len(required) <= 1 and kind in {"weather_like", "entity_symbol"}
     semantic_hints = _semantic_hints_for_query(q, kind)
-    alternates = _alternate_terms(q)
+    alternates = _alternate_terms(q) | set(_query_bases(query, q, kind)) | {_normalize(query), transliterate_cyrillic(query)}
     positive_context, negative_context = _context_terms(q, kind)
 
     variants: list[str] = []
-    quoted = f'"{q}"' if exact_phrase else q
-    variants.append(f'{quoted} latest news {freshness}')
+    bases = _query_bases(query, q, kind)
+
+    def _quoted(base: str) -> str:
+        return f'"{base}"' if len(base) >= 3 else base
+
+    for base in bases[:3]:
+        quoted_base = _quoted(base)
+        variants.append(f'{quoted_base} latest news {freshness}')
+
+    quoted = _quoted(q)
     # Keep the second/third paid search pass useful. Generic "breaking news" is often
     # too narrow for tech, esports and niche topics, so those kinds get a semantic
     # expansion before the generic fallback.
--- v53base/newsbot_v5_3_single_run_orchestrator/app/search_v2/article_filter.py	2026-04-26 08:59:39.000000000 +0000
+++ newsbot_v5_4_retrieval_reliability/app/search_v2/article_filter.py	2026-04-26 10:20:06.274851872 +0000
@@ -248,7 +248,13 @@
         threshold = 3 if plan.first_run else 5
     if score < threshold:
         return "topic_score_below_threshold"
-    if plan.topic_kind in {"country", "city", "geo_region", "person", "business", "crypto", "technology"} and title_hits == 0 and semantic_hits == 0:
+    if (
+        plan.topic_kind in {"country", "city", "geo_region", "person", "business", "crypto", "technology"}
+        and title_hits == 0
+        and semantic_hits == 0
+        and alt_hits == 0
+        and not bool(features["exact_phrase"])
+    ):
         return "indirect_relevance"
     if direct < plan.minimum_directness:
         return "low_directness"
--- v53base/newsbot_v5_3_single_run_orchestrator/app/infrastructure/sources/openrouter_web_search.py	2026-04-26 09:35:42.000000000 +0000
+++ newsbot_v5_4_retrieval_reliability/app/infrastructure/sources/openrouter_web_search.py	2026-04-26 10:20:31.616422382 +0000
@@ -244,21 +244,21 @@
                 {
                     "role": "system",
                     "content": (
-                        "You are a news discovery assistant. Search the web for genuinely recent news article candidates on the given topic. "
-                        "Prefer authoritative journalism and recent reporting with a visible publication date. Avoid old archive articles, "
-                        "evergreen pages, stale explainers, homepages, category pages and generic news hubs. If multiple languages "
-                        "cover the same story, prefer English then German. Return candidates; local validation will verify publication dates."
+                        "You are a news discovery assistant. Search the web for current news article candidates for the given search query. "
+                        "Prefer authoritative journalism, official reports, and pages that look like specific articles. Avoid old archive articles, "
+                        "evergreen pages, stale explainers, homepages, category pages and generic news hubs. Do not return zero solely because "
+                        "a publication date is not visible in the search result; local validation will verify dates and reject stale items."
                     ),
                 },
                 {
                     "role": "user",
                     "content": (
-                        f"Topic: {query}\n"
-                        f"Time window start: {from_dt.isoformat()}\n"
-                        f"Time window end: {to_dt.isoformat()}\n"
-                        f"Return up to {limit} specific news article links that appear to be published inside this window. "
-                        "Include the URL even if the date is not visible in the search result, but prefer candidates with visible dates. "
-                        "Use short one-line reasons and do not include homepages, encyclopedias, non-news explainers, organization landing pages, category pages, old archive pages, stock-analysis pages, or generic news-hub pages."
+                        f"Search query: {query}\n"
+                        f"Target freshness window start: {from_dt.isoformat()}\n"
+                        f"Target freshness window end: {to_dt.isoformat()}\n"
+                        f"Return up to {limit} specific article links that are likely to be current/recent. "
+                        "Prefer visible publication dates, but include strong article candidates even when the date is not shown in the search result. "
+                        "Use short one-line reasons. Exclude homepages, encyclopedias, non-news explainers, organization landing pages, category pages, old archive pages, stock-analysis pages, and generic news-hub pages."
                     ),
                 },
             ],
@@ -291,18 +291,18 @@
                 {
                     "role": "system",
                     "content": (
-                        "You are a news discovery assistant. Find genuinely recent news article candidates for the given topic. "
+                        "You are a news discovery assistant. Find current news article candidates for the given query. "
                         "Prefer authoritative sources with visible publication dates and cite each result. Avoid generic hubs and old archive pages."
                     ),
                 },
                 {
                     "role": "user",
                     "content": (
-                        f"Topic: {query}\n"
-                        f"Time window start: {from_dt.isoformat()}\n"
-                        f"Time window end: {to_dt.isoformat()}\n"
-                        f"Return up to {limit} specific news article links that appear to be published inside this window. "
-                        "Include the URL even if the date is not visible in the search result, but prefer candidates with visible dates. "
+                        f"Search query: {query}\n"
+                        f"Target freshness window start: {from_dt.isoformat()}\n"
+                        f"Target freshness window end: {to_dt.isoformat()}\n"
+                        f"Return up to {limit} specific article links that are likely to be current/recent. "
+                        "Prefer visible publication dates, but include strong article candidates even when the date is not shown in the search result. "
                         "Avoid homepages, encyclopedias, non-news explainers, organization landing pages, category pages, old archive pages, stock-analysis pages, and generic news-hub pages."
                     ),
                 },
@@ -786,9 +786,9 @@
             trusted = ('reuters', 'apnews', 'bbc', 'dw', 'ft.com', 'wsj', 'nytimes', 'theguardian', 'sky', 'bloomberg')
             if any(t in host for t in trusted):
                 return True
-            # Niche tech/esports/game/entertainment sites often do not use /news/YYYY URL shapes.
+            # Many legitimate article URLs do not use /news/YYYY URL shapes.
             # Let the topical quality gate decide when the title is direct enough.
-            if plan is not None and getattr(plan, "topic_kind", "") in {"technology", "esports", "game_updates", "entertainment", "team_org"}:
+            if plan is not None and getattr(plan, "topic_kind", "") in {"technology", "esports", "game_updates", "entertainment", "team_org", "business", "person", "city", "country", "crypto", "broad_single", "broad_dual"}:
                 if title_norm and len(re.findall(r"[a-zA-Z0-9а-яА-ЯёЁіІїЇєЄґҐ]+", title_norm)) >= 4:
                     return True
             return False

--- /dev/null	2026-04-26 08:49:46.684325620 +0000
+++ newsbot_v5_4_retrieval_reliability/tests/test_quality_engine_v54.py	2026-04-26 10:21:40.267139395 +0000
@@ -0,0 +1,62 @@
+from __future__ import annotations
+
+from datetime import datetime, timedelta, timezone
+
+from app.search_v2.article_filter import candidate_quality
+from app.search_v2.strategy import build_search_plan, canonicalize_topic, classify_topic
+
+
+def _plan(query: str):
+    to_dt = datetime(2026, 4, 26, 12, tzinfo=timezone.utc)
+    return build_search_plan(query, from_dt=to_dt - timedelta(days=7), to_dt=to_dt, first_run=True)
+
+
+def test_latin_and_cyrillic_city_forms_share_canonical_topic_and_queries() -> None:
+    latin = _plan("paris")
+    cyrillic = _plan("Париж")
+
+    assert latin.normalized_query == "paris"
+    assert cyrillic.normalized_query == "paris"
+    assert latin.topic_kind == "city"
+    assert cyrillic.topic_kind == "city"
+    assert any("paris france" in q.lower() for q in latin.query_variants[:3])
+    assert any("париж" in q.lower() for q in cyrillic.query_variants[:3])
+    assert "париж" in cyrillic.alternate_terms
+
+
+def test_cyrillic_brand_typo_can_retrieve_latin_brand_context() -> None:
+    plan = _plan("дайсан")
+
+    assert plan.normalized_query == "dyson"
+    assert plan.topic_kind == "business"
+    assert any('"dyson"' in q.lower() for q in plan.query_variants[:2])
+    assert "дайсан" in plan.alternate_terms
+
+
+def test_public_name_shorthand_normalizes_to_person_without_topic_specific_ranking() -> None:
+    for query in ["Trump", "Трамп", "Pavel Durov", "Павел Дуров"]:
+        plan = _plan(query)
+        assert plan.topic_kind == "person"
+        assert plan.normalized_query in {"donald trump", "pavel durov"}
+        assert len(plan.query_variants) >= 3
+
+
+def test_provider_freshness_hint_is_retrieval_broad_not_hard_date_filter() -> None:
+    plan = _plan("Dyson")
+
+    hint = plan.freshness_hint.lower()
+    assert "published after" not in hint
+    assert "recent news" in hint
+
+
+def test_cyrillic_alternate_terms_count_as_direct_topic_evidence() -> None:
+    plan = _plan("Париж")
+    quality = candidate_quality(
+        plan,
+        url="https://example.com/news/2026/04/26/paris-rentals",
+        title="Париж вводит новые ограничения для аренды жилья",
+        description="Власти Парижа объявили новые правила для туристического жилья.",
+    )
+
+    assert quality.accepted, quality.reason
+    assert quality.directness_score >= plan.minimum_directness

--- /dev/null	2026-04-26 08:49:46.684325620 +0000
+++ newsbot_v5_4_retrieval_reliability/QUALITY_ENGINE_V5_4_RETRIEVAL_RU.md	2026-04-26 10:23:39.328243236 +0000
@@ -0,0 +1,48 @@
+# Quality Engine v5.4 — Retrieval Reliability
+
+Цель v5.4 — не переписать движок, а исправить главный симптом из живых тестов: одна и та же сущность в другой форме ввода могла давать `0`, хотя новости существуют. Примеры из тестов: `paris` vs `Париж`, `дайсан` vs `Dyson`, `Trump`/`Трамп`.
+
+## Что изменено
+
+1. **Canonical topic normalization**
+   - `paris` / `Париж` приводятся к `paris` и получают city-контекст `France`.
+   - `дайсан` / `Дайсон` приводятся к `dyson`.
+   - `Trump` / `Трамп` приводятся к `donald trump`.
+   - `Павел Дуров` / `Pavel Durov` приводятся к `pavel durov`.
+
+2. **Multilingual query fan-out**
+   Движок теперь добавляет несколько форм темы в первые поисковые проходы: canonical, raw input и transliteration. Это должно уменьшить ситуацию, когда латиница даёт `0`, а кириллица находит новости, или наоборот.
+
+3. **Provider query теперь не душится жёстким `published after YYYY-MM-DD`**
+   OpenRouter иногда возвращал 0 результатов, если запрос слишком жёстко ограничивал дату. Теперь provider-запросы формулируются как `recent news from the last N days`, а реальную свежесть всё равно проверяет локальный freshness guard.
+
+4. **Cyrillic alternate terms считаются прямым evidence**
+   Если план нормализован в латиницу, но статья на кириллице прямо содержит исходную тему, это больше не считается `indirect_relevance`.
+
+5. **URL gate немного смягчён для реальных статей**
+   Для business/person/city/country/crypto/broad тем движок не отбрасывает статью только потому, что URL не содержит `/news/` или дату, если заголовок достаточно похож на конкретную статью. Freshness и quality filters всё равно остаются.
+
+## Что НЕ изменено
+
+- Бюджетный потолок не увеличен.
+- Freshness guard не отключен.
+- Старые статьи не должны проходить как свежие.
+- Generic news hub pages всё ещё режутся.
+- Это не подгонка под конкретные тестовые темы; это общий слой нормализации и query reliability.
+
+## Что тестировать
+
+Проверь пары:
+
+- `paris` и `Париж`
+- `Dyson` и `дайсан` / `Дайсон`
+- `Trump` и `Трамп`
+- `Pavel Durov` и `Павел Дуров`
+- латинские/кириллические варианты городов и брендов
+
+Важно смотреть не только количество новостей, но и:
+
+- не пошли ли старые статьи;
+- не пролезли ли hub pages;
+- не слишком ли слабые источники;
+- различается ли результат при смене языка ввода.

--- /dev/null	2026-04-26 08:49:46.684325620 +0000
+++ newsbot_v5_4_retrieval_reliability/START_HERE_V5_4_RU.md	2026-04-26 10:23:39.406343783 +0000
@@ -0,0 +1,25 @@
+# Как запускать v5.4
+
+1. Распакуй архив.
+2. Проверь `.env`. Новых обязательных переменных в v5.4 нет, но значения из v5.3 должны остаться:
+
+```env
+OPENROUTER_WEB_SEARCH_MAX_RESULTS=15
+OPENROUTER_WEB_SEARCH_RESULTS_PER_CALL=5
+OPENROUTER_WEB_SEARCH_MAX_CALLS_PER_TOPIC=3
+OPENROUTER_TOPIC_BUDGET_USD=0.07
+OPENROUTER_ALLOW_SEARCH_SNIPPET_FALLBACK=true
+OPENROUTER_MAX_WEAK_FRESHNESS_ITEMS_PER_DIGEST=2
+OPENROUTER_REQUIRE_VERIFIED_PUBLISH_DATE=true
+OPENROUTER_ALLOW_UNDATED_FALLBACK=false
+OPENROUTER_DEBUG_REJECTIONS=true
+IMMEDIATE_REPEAT_GUARD_MINUTES=15
+```
+
+3. Желательно прогнать:
+
+```bash
+pytest -q tests/test_quality_engine_v54.py tests/test_quality_engine_v53.py tests/test_freshness_guard_v51.py tests/test_quality_engine_v52.py
+```
+
+4. Затем тестируй живые темы парами: латиница / кириллица.

--- /dev/null	2026-04-26 08:49:46.684325620 +0000
+++ newsbot_v5_4_retrieval_reliability/TEST_REPORT_V5_4.md	2026-04-26 10:23:39.416532046 +0000
@@ -0,0 +1,18 @@
+# Test report v5.4
+
+Проверено в этой среде:
+
+```text
+python -m compileall -q app tests: PASS
+manual deterministic v5.4 tests: PASS
+```
+
+Новый deterministic-тест без живого OpenRouter проверяет:
+
+- `paris` и `Париж` имеют общий canonical topic;
+- `дайсан` нормализуется в `dyson`;
+- `Trump`/`Трамп` и `Pavel Durov`/`Павел Дуров` классифицируются как `person`;
+- provider freshness hint больше не использует жёсткий `published after YYYY-MM-DD`;
+- кириллическое совпадение темы считается direct evidence, а не `indirect_relevance`.
+
+Живой OpenRouter/Telegram здесь не прогонялся: нет твоих ключей и runtime-окружения.
